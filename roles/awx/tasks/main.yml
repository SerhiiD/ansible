---
# tasks file for awx

#- name: install by pip
#  pip:
#    name: ansible
#    state: latest
#
#- name: install the 'Development tools' package group
  #yum:
    #name: "@Development tools"
    #state: present

- name: install packages by yum
  yum:
    name:
      - docker
      - "@Development tools"
      - gettext
      - python-devel
      - libevent-devel
    state: latest
    update_cache: yes
